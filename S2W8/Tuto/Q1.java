package S2W8.Tuto;

public class Q1 {
    // Queue follows a strict FIFO order, whereas 
    // PriorityQueue uses a priority-based ordering mechanism where elements with higher priority are dequeued first.
}
