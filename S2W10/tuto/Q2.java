package S2W10.tuto;

public class Q2 {
    /*
     * The problem occurs is the compile-time error. 
     * This happens because the method f() requires an integer parameter n but the function call does not pass in an integer argument. 
     * This error happens when the compiler expects the f() method call to accept exactly one integer parameter but the argument is not provided.
     */
}
