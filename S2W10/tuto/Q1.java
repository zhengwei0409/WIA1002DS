package S2W10.tuto;

public class Q1 {
    /*
     * The recursive method runs infinitely because the value of n will always be lower than the base case of n == 1, 
     * so it will cause the program's stack to continuously grow until it reaches a particular height 
     * and the StackOverflowError exception is initiated which crashes the program.
     */
}
